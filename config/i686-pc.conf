[constants]
arch = 'i686-pc-elf'

[binaries]
c = arch + '-gcc'
ar = arch + '-ar'
ranlib = arch + '-ranlib'
strip = arch + '-strip'

[host_machine]
system = 'linux'
cpu_family = 'x86'
cpu = 'i686'
endian = 'little'